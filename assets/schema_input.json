{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/Biocentric/nf-se-to-pe/main/assets/schema_input.json",
    "title": "nf-se-to-pe pipeline - params.input schema",
    "description": "Schema for the samplesheet used as input.",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces."
            },
            "fastq": {
                "type": "string",
                "format": "file-path",
                "pattern": "^\\S+\\.f(ast)?q(\\.gz)?$",
                "errorMessage": "FASTQ file must be provided and end with .fastq(.gz) or .fq(.gz)."
            }
        },
        "required": ["sample", "fastq"]
    }
}
